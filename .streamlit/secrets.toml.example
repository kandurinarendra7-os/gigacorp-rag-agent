# Rename this file to secrets.toml (remove .example) for local dev,
# or paste this content into the "Secrets" box in Streamlit Community Cloud.
# You only need ONE of these depending on which provider you pick in the app sidebar.

OPENAI_API_KEY = "sk-...your-key-here..."
ANTHROPIC_API_KEY = "sk-ant-...your-key-here..."
